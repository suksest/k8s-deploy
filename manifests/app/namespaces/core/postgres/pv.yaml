apiVersion: v1
kind: PersistentVolume
metadata:
  name: postgres-volume
  namespace: core
  labels:
    type: local
    app: postgres
spec:
  storageClassName: do-block-storage
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: /data/postgresql
